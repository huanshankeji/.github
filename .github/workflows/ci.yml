name: CI

on:
  workflow_call:
    inputs:
      jdk-versions:
        description: "Comma-separated JDK versions passed to setup-javas"
        required: true
        type: string
      runs-on:
        description: "Runner label for both jobs"
        required: false
        type: string
        default: macos-latest
    secrets:
      GH_ACTOR:
        required: false
      GH_TOKEN:
        required: false
      GITLAB_PRIVATE_TOKEN:
        required: false

jobs:
  check:
    runs-on: ${{ inputs.runs-on }}
    permissions:
      contents: read
    steps:
      - uses: huanshankeji/.github/actions/gradle-check@improve-cross-repo-dependency
        with:
          jdk-versions: ${{ inputs.jdk-versions }}
          gh-actor: ${{ secrets.GH_ACTOR }}
          gh-token: ${{ secrets.GH_TOKEN }}
          gitlab-private-token: ${{ secrets.GITLAB_PRIVATE_TOKEN }}

  dependency-submission:
    runs-on: ${{ inputs.runs-on }}
    permissions:
      contents: write
    steps:
      - uses: huanshankeji/.github/actions/gradle-dependency-submission@improve-cross-repo-dependency
        with:
          jdk-versions: ${{ inputs.jdk-versions }}
